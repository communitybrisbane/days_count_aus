{
  "name": "days-count-aus-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.0",
    "nodemailer": "^8.0.3"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.11",
    "typescript": "^5.4.0"
  },
  "private": true
}
